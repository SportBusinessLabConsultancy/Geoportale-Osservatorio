var custom = {
    main_page_columns: 3, // 1 or 3 
    zoom_on_click: true, // center the clicked geometry and shows it in the optimal zoom
    mouse_buffer: 0, // use buffer of [mouse_buffer] pixels around clicked point to make it easier to select futures
    popup_on_click: true, // open popup on mouse click on features with popup_status=true
    popup_on_hover: false, // show a tooltip on mouse over on features with popup_status=true
    layer_show_hide: false, // activate layer toggle on legend click
}